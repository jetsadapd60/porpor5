# ระบบจัดเก็บข้อมูลไฟล์ ปพ.5 (รายวิชา) สำหรับครู

## 1. บทนำ
ระบบสำหรับให้ครูผู้สอนสามารถอัปโหลด จัดเก็บ และค้นหาไฟล์ ปพ.5 (เอกสารบันทึกผลการพัฒนาคุณภาพผู้เรียนรายวิชา) เพื่อความสะดวกในการเรียกดู เป็นระเบียบ และป้องกันข้อมูลสูญหาย

## 2. ความต้องการของระบบ (System Requirements)

### 2.1 การนำเข้าและจัดเก็บข้อมูล (Upload & Storage)
- ครูผู้สอนสามารถอัปโหลดไฟล์ ปพ.5 เข้าสู่ระบบได้
- รองรับไฟล์ `.pdf`
- จำกัดขนาดไฟล์ต่อการอัปโหลด (เช่น ไม่เกิน 5MB)
- สามารถระบุข้อมูลประกอบ (Metadata) ของไฟล์ ปพ.5 เพื่อใช้ในการจัดหมวดหมู่ ดังนี้:
  - ปีการศึกษา (เช่น 2569)
  - ภาคเรียน (1 หรือ 2)
  - รหัสวิชา และ ชื่อวิชา
  - ระดับชั้น (เช่น ม.1)
  - ห้องเรียน (เช่น ม.1/1)
  - ชื่อครูผู้สอน (ผู้ใช้อัปโหลดกรอกชื่อเองทุกครั้ง)

### 2.2 การค้นหาและเรียกดูข้อมูล (Search & Retrieval)
- **การค้นหาและกรองข้อมูล (Search & Filter):** 
  - **Admin:** สามารถค้นหาและกรองไฟล์ ปพ.5 ได้จากทุกรายวิชาในระบบ
  - **User (ครูผู้สอน):** ค้นหาและดูได้เฉพาะไฟล์ที่ตนเองรับผิดชอบเท่านั้น
  - โดยสามารถกรองจากเงื่อนไขต่างๆ เช่น: ปีการศึกษา, ภาคเรียน, รหัสวิชา, ชื่อวิชา, ระดับชั้น และชื่อครูผู้สอน
- **การเรียกดูและดาวน์โหลด (View & Download):**
  - สามารถดาวน์โหลดไฟล์ต้นฉบับลงมายังเครื่องคอมพิวเตอร์ได้ (ตามสิทธิ์การเข้าถึง)
  - สามารถดูตัวอย่างไฟล์ (Preview) บนเบราว์เซอร์ได้โดยตรง (สำหรับไฟล์ PDF)

### 2.3 การจัดการและการแก้ไข (Management)
- ผู้ใช้งานสามารถลบหรืออัปโหลดไฟล์ ปพ.5 ใหม่ทับไฟล์เดิมได้ (กรณีส่งผิดหรือต้องการอัปเดตข้อมูล)
- ระบบมีการบันทึกวันที่และเวลาที่มีการอัปโหลดไฟล์ล่าสุด

### 2.4 สิทธิ์การเข้าถึงและความปลอดภัย (Access Control & Security)
- **ระบบยืนยันตัวตน (Authentication):** ผู้ใช้งานต้องเข้าสู่ระบบ (Login) ด้วยบัญชี Google (Google Sign-In) ก่อนเข้าใช้งานระบบ
- **การกำหนดสิทธิ์ (Authorization) และความเป็นเจ้าของไฟล์ (Ownership):**
  - ทุกครั้งที่มีการอัปโหลด ระบบจะบันทึก Email หรือ User ID จาก Google Sign-in เข้ากับไฟล์นั้นในฐานข้อมูล (Supabase) ไว้เบื้องหลัง เพื่อใช้ตรวจสอบสิทธิ์การเป็นเจ้าของไฟล์
  - ระบบแบ่ง Role ผู้ใช้งานเป็น 2 ระดับ ได้แก่
    - **User (ครูผู้สอน):** สามารถอัปโหลด แก้ไข ลบ และดาวน์โหลดไฟล์ ปพ.5 เฉพาะไฟล์ที่ผูกกับบัญชี Email ของตนเองเท่านั้น
    - **Admin (ผู้ดูแลระบบ/ฝ่ายวิชาการ):** สามารถค้นหา ดู ดาวน์โหลด และจัดการไฟล์ ปพ.5 ของทุกรายวิชาในระบบได้ทั้งหมด (สิทธิ์ Admin จะถูกกำหนดโดยการเพิ่มรายชื่อ Email ไว้ล่วงหน้าในฐานข้อมูล Supabase)
- **การจัดเก็บไฟล์:** ไฟล์ ปพ.5 (PDF) ทั้งหมดจะต้องถูกจัดเก็บไว้บน Google Drive **ส่วนกลางของโรงเรียน ผ่านทางการใช้ Google Cloud Service Account** (ระบบจะอัปโหลดและจัดการไฟล์โดยไม่ต้องให้ผู้ใช้ Auth Drive ส่วนตัว) โดยระบบจะต้องจัดโครงสร้างโฟลเดอร์อัตโนมัติตามลำดับคือ **ปีการศึกษา > ระดับชั้น > ห้อง** (เช่น 2569 > ม.1 > ม.1/1)
- ระบบมีการสำรองข้อมูล (Backup) อย่างสม่ำเสมอเพื่อป้องกันข้อมูลสูญหาย

## 3. รายงานสรุป (Reports)
- ระบบมีหน้ารายงานสรุปรายการไฟล์ ปพ.5 ที่ถูกอัปโหลดเข้ามาในแต่ละภาคเรียน
- สามารถดูภาพรวมและจัดกลุ่มตามรายวิชา ระดับชั้น ห้อง หรือชื่อครูผู้สอน เพื่อให้ง่ายต่อการติดตาม

## 4. เทคโนโลยีที่ใช้ (Tech Stack)
- **แนวคิดหลัก:** เน้นสถาปัตยกรรมที่เบาและทำงานได้รวดเร็ว (Lightweight & Fast)
- **Frontend:** Svelte (เพื่อประสิทธิภาพความเร็วและขนาดไฟล์ที่เล็ก)
- **Styling:** Tailwind CSS (เน้นการออกแบบ UI สไตล์ Modern สวยงาม ทันสมัย และสะอาดตา)
- **Responsive Design:** รองรับการใช้งานได้อย่างสมบูรณ์แบบบนทุกอุปกรณ์ (มือถือ, แท็บเล็ต และคอมพิวเตอร์)
- **Backend & Database:** Supabase (ใช้เป็นฐานข้อมูลหลักในการเก็บ Metadata และจัดการระบบ Authentication)
- **File Storage:** Google Drive API (ทำงานร่วมกับ Service Account เพื่ออัปโหลดไฟล์ไปที่ Drive ส่วนกลางของโรงเรียนและจัดการโฟลเดอร์อัตโนมัติ)
- **Deployment:** Hostinger (Ubuntu OS) โดยรันแอปพลิเคชัน Frontend ในรูปแบบ Docker Container และใช้งานร่วมกับ Nginx (ที่มีอยู่แล้วบน Server) ในฐานะ Reverse Proxy
- **CI/CD:** GitHub Actions (สำหรับการ Build Docker Image และ Deploy ขึ้น Hostinger อัตโนมัติ)
